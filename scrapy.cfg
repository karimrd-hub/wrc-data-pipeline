# Automatically created by: scrapy startproject
#
# For more information about the [deploy] section see:
# https://scrapyd.readthedocs.io/en/latest/deploy.html
# concurrency, middlewares, pipelines
[settings]
default = wrc_pipeline.scrapers.settings
# Dagster runs the spider as a subprocess directly, so you don't need Scrapyd.
[deploy]
#url = http://localhost:6800/
project = wrc_pipeline
